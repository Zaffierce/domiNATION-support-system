<%- include('../../partials/head.ejs'); %>
  <title>User: <%= user.username %> - DomiNATION Ark: Survival Evolved Community</title>
<link rel="stylesheet" type="text/css" href="../../styles/forms.css">
<script src="/js/app.js" type="text/javascript"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-validator/0.5.3/css/bootstrapValidator.min.css" integrity="sha256-gBxd65TVOCRCDC48+AXN6hhg3I/fZqE/BnwgKwdzJG4=" crossorigin="anonymous" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-validator/0.5.3/js/bootstrapValidator.min.js" integrity="sha256-PbFF1Mdg86urwOYXWNJPP4z5Ge9KLp6KXX1NURQY8Ho=" crossorigin="anonymous"></script>
</head>
<body>
  <%- include('../../partials/nav.ejs'); %>
    <div class="container">
        <div class="row greybox">
          <div class="col infobox">
            <div>
              <strong>Submitted on:</strong> <%= ticket.submitted_on %><br />
              <strong>Status:</strong> <%= ticket.status %><br />
              <strong>Username:</strong> <%= ticket.ign %><br />
              <strong>Discord ID:</strong> <%= ticket.discord_name %><br />
              <br />
              <hr />
              <strong>Pickup Details</strong><br />
              <strong>Server ID:</strong>  <%= ticket.serverid_pickup %><br />
              <strong>CCC:</strong> <%= ticket.server_pickup_location %><br />
              <strong>Amount:</strong> <%= ticket.transfer_amount %><br />
              <br />
              <hr />
              <strong>Dropoff Details</strong><br />
              <strong>Server ID:</strong>  <%= ticket.serverid_dropoff %><br />
              <strong>CCC:</strong> <%= ticket.server_dropoff_location %><br />
            </div>
            <br />
            <div class="text-center">
              <div class="row">
              <% if (user.isAdmin && ticket.status === "NEW") { %>
                <div class="col-sm-6">
                  <form method="POST" action="/cancel/3/<%=ticket.id%>">
                    <input type="submit" class="btn btn-danger btn-lg" value="Cancel ticket">
                  </form>
                </div>
                <div class="col-sm-6">
                  <form method="POST" action="/open/3/<%=ticket.id%>">
                    <input type="submit" class="btn btn-success btn-lg" value="Accept ticket">
                  </form>
                </div>
              <% } else if (user.isAdmin && ticket.status === "OPEN") { %>
                <div class="col-sm-6">
                  <form method="POST" action="/cancel/3/<%=ticket.id%>">
                    <input type="submit" class="btn btn-danger btn-lg" value="Cancel ticket">
                  </form>
                </div>
                <div class="col-sm-6">
                  <form method="POST" action="/complete/3/<%=ticket.id%>">
                    <input type="submit" class="btn btn-success btn-lg" value="Complete ticket">
                  </form>
                </div>
              <% } else if (ticket.status === "NEW" || ticket.status === "OPEN") { %>
                <div class="col">
                  <form method="POST" action="/cancel/3/<%=ticket.id%>">
                    <input type="submit" class="btn btn-danger btn-lg" value="Cancel ticket">
                  </form>
                </div>
              <% } else if (ticket.status === "COMPLETE") { %>
                <div class="col">
                  This ticket was completed by <%= ticket.closed_by %> on <%= ticket.closed_on %>.
                </div>
              <% } else { %>
                <div class="col">
                  This ticket was cancelled by <%= ticket.closed_by %> on <%= ticket.closed_on %>.
                </div>
              <% } %>
            </div>
            </div>
          </div>
        </div>
    </div>  
</div> <!--Close of content-->

</body>
</html>